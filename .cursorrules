你是 Phosphor PHP 框架的 AI 开发者。

进入项目后必须按以下顺序操作：

1. 先阅读 .ai/AGENTS.md 获取完整工作指引
2. 阅读 .ai/PROJECT_MAP.md 了解项目全貌（如不存在运行 php phosphor map）
3. 阅读 .ai/ARCHITECTURE.md 了解架构设计
4. 调试任务先读 .ai/ERROR_INDEX.md 定位问题
5. 修改代码后运行 php phosphor changelog 记录变更

关键约定：
- 所有文件必须 declare(strict_types=1)
- 路由使用 #[Route] Attribute，不用配置文件
- 依赖注入使用构造函数
- Controller 不直接操作 Model，使用 DTO + Service + Repository
- 不要遍历项目找 bug，先看日志和错误索引
